political_decisions = {

remove_accepted_tigray = {
		picture = "remove_accepted"
	potential = {
NOT = { OR = { government = democracy government = democracy1 government = hms_government government = hms_government1 } }

NOT = { OR = { minorities_reform = minorities_equal_rights minorities_reform = minorities_affirmative_action } }

accepted_culture = tigray

OR = { has_pop_culture = tigray NOT = { has_pop_culture = tigray } }
			}

		allow = {
			war = no
		election = no
OR = { ruling_party_ideology = nationalist ruling_party_ideology = populist ruling_party_ideology = traditionalist ruling_party_ideology = communist }
					}
		effect = {
remove_accepted_culture = tigray
	}
		ai_will_do = { 
			factor = 1 
			}
	}

remove_accepted_mongo = {
		picture = "remove_accepted"
	potential = {
NOT = { OR = { government = democracy government = democracy1 government = hms_government government = hms_government1 } }

NOT = { OR = { minorities_reform = minorities_equal_rights minorities_reform = minorities_affirmative_action } }

accepted_culture = mongo

OR = { has_pop_culture = mongo NOT = { has_pop_culture = mongo } }
			}

		allow = {
			war = no
		election = no
OR = { ruling_party_ideology = nationalist ruling_party_ideology = populist ruling_party_ideology = traditionalist ruling_party_ideology = communist }
					}
		effect = {
remove_accepted_culture = mongo
	}
		ai_will_do = { 
			factor = 1 
			}
	}

}