political_decisions = {
	population_growth = {
		alert = no
		picture = "population_growth"
		potential = {
			NOT = {
				has_country_modifier = baby_boom
			}
		}

		allow = {
		
			#Favourable local conditions
			has_recently_lost_war = no
			war = no
			election = no
			NOT = { average_militancy = 3 }
			
			
			#Must have money to spend
			money = 100000

			#Traditionalist, nationalist, populist, communist governments with either nationalist, traditionalist, populist, or communist pop majority ideologies only
			OR = {
				AND = {
					OR = {
						ruling_party_ideology = traditionalist
						ruling_party_ideology = nationalist
						ruling_party_ideology = populist
					}
					OR = {
						pop_majority_ideology = traditionalist
						pop_majority_ideology = nationalist
						pop_majority_ideology = populist						
					}
				}
				AND = {
					OR = {
						ruling_party_ideology = communist
						ruling_party_ideology = communist_communist
					}
					OR = {
						pop_majority_ideology = communist_social
						pop_majority_ideology = communist
					}
				}
				#Allow powerful nations like India under the INC to take the decision
				AND = {
					OR = {
						is_greater_power = yes
						is_secondary_power = yes
					}
					OR = {
						ruling_party_ideology = socialist
						ruling_party_ideology = progressive
					}
					OR = {
						pop_majority_ideology = socialist
						pop_majority_ideology = progressive
					}
				}
			}
		}

		effect = {
			add_country_modifier = { name = baby_boom duration = 1780 }
			any_pop = { consciousness = 5 }
		}

		ai_will_do = {
			factor = 1
			
			#Only GP AIs take this decision
			modifier = { factor = 0 is_greater_power = no }
		}
		
	}
}
