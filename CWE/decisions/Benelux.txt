political_decisions = {

	form_benelux = {
		picture = "form_benelux"
		potential = {
			OR = { tag = NET tag = WLL tag = FLA tag = BEL tag = LUX }

			OR = { 
				AND = { NOT = { tag = NET } exists = NET }
				AND = { NOT = { tag = WLL } exists = WLL }
				AND = { NOT = { tag = FLA } exists = FLA }
				AND = { NOT = { tag = BEL } exists = BEL }
				AND = { NOT = { tag = LUX } exists = LUX } 
			}

			NOT = {
				exists = BNL
			}
		}
		
		allow = {
			is_greater_power = yes
			prestige = 150
			NOT = { ruling_party_ideology = nationalist }
			war = no

			OR = { tag = NET NOT = { exists = NET } AND = { NOT = { tag = NET } OR = { is_sphere_leader_of = NET is_our_vassal = NET } } }

			OR = { tag = WLL NOT = { exists = WLL } AND = { NOT = { tag = WLL } OR = { is_sphere_leader_of = WLL is_our_vassal = WLL } } }

			OR = { tag = FLA NOT = { exists = FLA } AND = { NOT = { tag = FLA } OR = { is_sphere_leader_of = FLA is_our_vassal = FLA } } }

			OR = { tag = BEL NOT = { exists = BEL } AND = { NOT = { tag = BEL } OR = { is_sphere_leader_of = BEL is_our_vassal = BEL } } }

			OR = { tag = LUX NOT = { exists = LUX } AND = { NOT = { tag = LUX } OR = { is_sphere_leader_of = LUX is_our_vassal = LUX } } }

		}
		
		effect = {
			prestige = 20
			change_tag = BNL
			NET = { all_core = { add_core = THIS } }
			BEL = { all_core = { add_core = THIS } }
			LUX = { all_core = { add_core = THIS } }
			#WLL = { all_core = { add_core = THIS } }
			#FLA = { all_core = { add_core = THIS } }
			#add_accepted_culture = wallonian
			#add_accepted_culture = flemish
			#add_accepted_culture = dutch
			#add_accepted_culture = german
			any_country = {
				limit = {
					OR = { tag = NET tag = WLL tag = FLA tag = BEL tag = LUX }
					in_sphere = THIS	
				}
				country_event = { id = 11101 }
			}
			# Fire events choosing state of the monarchy if any of those states are monarchies, and for choosing the capital
		}
		ai_will_do = {
			factor = 1
		}
	}

	form_benelux2 = {
		picture = "form_benelux"
		potential = {
			AND = { 
				exists = NET 
				exists = BEL 
				exists = LUX 
				NOT = { exists = BNL }
				NOT = { exists = XEU }		
				NOT = { exists = FLA }		
				NOT = { exists = WLL }	
				OR = { government = democracy government = hms_government }
			}

			OR = { tag = NET tag = BEL tag = LUX }
		}
		
		allow = {
			war = no
			is_greater_power = yes
			prestige = 150
			NOT = { ruling_party_ideology = nationalist }

			AND = {
				OR = { is_sphere_leader_of = NET is_our_vassal = NET tag = NET }
				OR = { NET = { government = democracy } NET = { government = hms_government } }
			}
			AND = {
				OR = { is_sphere_leader_of = BEL is_our_vassal = BEL tag = BEL }
				OR = { BEL = { government = democracy } BEL = { government = hms_government } }
				}
			AND = {
				OR = { is_sphere_leader_of = LUX is_our_vassal = LUX tag = LUX }
				OR = { LUX = { government = democracy } LUX = { government = hms_government } }
			}
		}
		
		effect = {
			prestige = 20
			change_tag = BNL
			add_accepted_culture = dutch
			add_accepted_culture = flemish
			add_accepted_culture = wallonian
			add_accepted_culture = german
			remove_accepted_culture = french 
			any_country = {
				limit = {
					in_sphere = THIS
					OR = { tag = NET tag = BEL tag = LUX }
				}
				country_event = { id = 11101 }
			}
			NET = { all_core = { add_core = THIS } }
			BEL = { all_core = { add_core = THIS } }
			LUX = { all_core = { add_core = THIS } }
			
			country_event = { id = 7000001 }
			country_event = { id = 7000002 }
		}
		ai_will_do = {
			factor = 0
		}
	}


}